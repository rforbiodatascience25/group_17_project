---
title: "Rasmus_test"
format: html
---

```{r}
read.csv("data/otu_table_16S_rhi_W5.csv",header=T,row.names=1,sep = ";")
```

```{r}
library(readr)
library(here)
```

```{r}
# Reading the contents of TSV file using read_tsv() method
df<-readr::read_tsv(here("data/support.tsv"))
summary(df)
```

```{r}
load(here("data/bacteremia.sav"))
```

```{r}
plasma <- read_sav(here("data/plasma.sav"))
View(plasma)
```
