---
title: "Main"
format: html
---

```{r}
library("tidyverse")
library("readxl")
library("here")
library("readr")
```

```{r}
data<-read_tsv(here("data/support.tsv"))
data
```

```{r}
data |> group_by(income) |> summarize(n = n())
```

```{r}
data
```
